# Mean Absolute Percentage Error Loss Configuration
# Useful when relative errors matter more than absolute errors
# Returns loss as percentage (0-100 scale)

name: "mape"
epsilon: 1e-8                 # Prevents division by zero
reduction: "mean"
