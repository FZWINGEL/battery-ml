# BatteryML does not require environment variables by default.
#
# Optional overrides (uncomment and adjust if desired):
# BATTERYML_DATA_ROOT=Raw Data
# MLFLOW_TRACKING_URI=file:./artifacts/mlruns
# MLFLOW_EXPERIMENT_NAME=battery_degradation
